"""
vectordb — a vector database built from scratch using only numpy.

Provides multiple ANN index types (Flat, IVF, PQ, HNSW) behind a common
VectorIndex interface, plus shared utilities for distance computation,
k-means clustering, and index persistence.
"""
